// var cmsAnimations = angular.module('CmsAnimations', ['ngAnimate']);
/*cmsAnimations.animation('.fadeInRight', function () {
  return {
    enter: function(element, done) {
      element.css({
        left: "100%"
      }).animate({
        left:0    
      },1000, done);
    }
  };
});*/